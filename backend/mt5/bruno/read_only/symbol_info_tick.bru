meta {
  name: Symbol tick
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/symbol_info_tick/{{symbol}}
  body: none
  auth: none
}

tests {
  test("returns 200", function () {
    expect(res.status).to.equal(200);
  });
}
